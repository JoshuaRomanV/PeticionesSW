{
  "name": "app-de-notas-progressiva",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "devDependencies": {
    "jest": "^27.2.1"
  },
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "test": "jest",
    "test:ci": "jest --watch=false --browsers=ChromeHeadlessNoSandbox",
    "eject": "vite eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "jest": {
    "testEnvironment":"jsdom"

  }
}
